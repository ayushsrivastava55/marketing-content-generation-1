export const AI_CONFIG = {
  model: 'gpt-4o',
  temperature: 0.7,
  maxTokens: 2000,
  retryAttempts: 3,
  retryDelay: 1000,
}

export const DEFAULT_HEADERS = {
  'Content-Type': 'application/json',
} 